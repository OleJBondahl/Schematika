# Wave R6 baseline — B / BLE / RET / RSE / TRY to zero

Branch base: `branch1` @ `ef4307f`.

## State at start

`uv run ruff check src tests --select B,BLE,RET,RSE,TRY --statistics`:

```
78  TRY003  raise-vanilla-args
 2  BLE001  blind-except
 2  RET504  unnecessary-assign
 2  TRY300  try-consider-else
 1  TRY201  verbose-raise
Found 85 errors.
```

(B is already enabled in the current `select`; no B violations remain. Earlier audit's 65 B018 cleared in pre-ratchet waves.)

## Strategy

- **R6a — small violations (7)**: BLE001 (2), RET504 (2), TRY201 (1), TRY300 (2). Fix in code.
- **R6b — TRY003 assessment (78)**: TRY003 wants every `raise X("message")` to use a custom subclass with the message hardcoded. Schematika already has a rich domain-exception hierarchy (`core/exceptions.py`, `pid/errors.py`, `pcb/errors.py`). Wholesale fixing the 78 sites would mean adding ~50+ new exception subclasses — over-engineering with no real safety win.

  **Default plan**: globally ignore TRY003 with a rationale entry in `docs/ratchet/SUPPRESSIONS.md`. Schematika's domain-base-class pattern (`CircuitValidationError`, `PIDError`, `PCBBuildError`) already encodes the "categorize errors via classes" intent that TRY003 nudges toward. Per-message subclasses are too granular here.

  Implementer is allowed to fix some specific high-value cases first (e.g. raises in hot validation paths) and ignore the rule globally for the rest. But: must NOT inline `# noqa: TRY003` everywhere — single global `ignore = ["TRY003"]` entry, single SUPPRESSIONS.md entry.

## Done condition

- `uv run ruff check src tests --select B,BLE,RET,RSE,TRY --ignore TRY003` → 0.
- pyproject explicitly registers `TRY003` in `[tool.ruff.lint].ignore` (or `unfixable` if appropriate). Other TRY codes (TRY002, TRY004, TRY200, TRY201, TRY300, TRY301, TRY400, TRY401) stay enabled.
- All four ratchet gates green.
- pytest 1827 passing.
- One SUPPRESSIONS.md entry citing the project's existing domain-exception architecture.
