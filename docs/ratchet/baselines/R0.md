# Wave R0 baseline — Format clean

Snapshot at start of Wave R0. Branch base: `branch1` @ `0cb6aee`.

## Target

Drive `ruff format --check src tests tools` to clean.

## State at start

`uv run ruff format --check src tests tools` reports:

```
Would reformat: tools\cad_parser\__main__.py
Would reformat: tools\cad_parser\parsers\autocad.py
Would reformat: tools\cad_parser\parsers\kicad.py
Would reformat: tools\cad_parser\parsers\pdf.py
Would reformat: tools\cad_parser\parsers\svg.py
5 files would be reformatted, 197 files already formatted
```

`src/` and `tests/` are already format-clean. The 5 dirty files all live under `tools/cad_parser/`.

## Notes

- The current pre-commit hook `ruff-format` filters on `files: ^(src|tests)/`, which is why these 5 tool files weren't kept clean by the hook. Extending the hook to also cover `tools/` is a Wave L1 concern (pre-commit hardening), not R0.
- No semantic changes — pure whitespace/formatting.

## Done condition

- `uv run ruff format --check src tests tools` exits 0.
- `just ci` passes.
- Diff touches only the 5 listed files.
