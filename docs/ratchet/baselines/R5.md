# Wave R5 baseline — N-series naming to zero

Branch base: `branch1` @ `5f41aca`.

## State at start

`uv run ruff check src tests --select N`:

- **27 violations**: 23 N806, 4 N817

### Breakdown

src/ (8):
- `pid/layout.py:133` — `WHITE`, `GRAY`, `BLACK` (color constants, in-function)
- `rendering/typst/frame_generator.py:44–77` — `WIDTH`, `HEIGHT`, `COLS`, `ROWS` (page geometry, in-function)
- (1 unaccounted; reviewer to confirm)

tests/ (19):
- `tests/unit/test_pcb_adapter.py` — many `F1`, `F2`, `T1`, `_F1` etc. — IEC-style fuse/terminal naming, intent-bearing.
- `tests/unit/test_plc_resolver.py:323` — `DI_MODULE_1CH` (PLC module identifier).
- `tests/unit/test_plc_resolver.py:562–572` — N817: `PD`, `PMT`, `PR` import aliases.
- `tests/unit/test_terminal_type.py:90` — N817: `T` import alias for `Terminal`.

## Strategy

- **src/ (8 violations)**: lowercase OR hoist to module-level `Final` constants (then they pass naming convention as MODULE_LEVEL caps are fine).
- **tests/ test_pcb_adapter.py**: add `"tests/unit/test_pcb_adapter.py" = ["N806"]` to per-file-ignores. Document in SUPPRESSIONS.md as "IEC-style fuse/terminal designators (F1, T1) are part of the test's domain semantics."
- **tests/test_plc_resolver.py**: same pattern with N806 + N817; one PLC module identifier and three import aliases.
- **tests/test_terminal_type.py**: N817 `T` alias justified for Terminal — add per-file-ignore.

This is a controlled expansion of `[per-file-ignores]` for test files where domain naming wins. Each entry must be justified in `docs/ratchet/SUPPRESSIONS.md`.

## Done condition

- `uv run ruff check src tests --select N` → 0.
- All four ratchet gates green.
- pytest unchanged (1827 passing).
- All new per-file-ignore entries documented in `docs/ratchet/SUPPRESSIONS.md`.
- No `# noqa: N806` inline (use per-file-ignores instead — easier to audit).
