# Wave R7 baseline — PLR + C90 complexity

Branch base: `branch1` @ `35f30ab`.

## State at start

`uv run ruff check src tests --select PLR,C90 --statistics`:

```
25  PLR2004  magic-value-comparison       (mostly mechanical — extract to constants)
18  C901     complex-structure            (refactor or raise threshold)
10  PLR0913  too-many-arguments           (>8 args; bundle via dataclass/Builder)
 9  PLR0912  too-many-branches            (refactor or raise threshold)
 5  PLR0915  too-many-statements          (refactor or raise threshold)
 2  PLR0911  too-many-return-statements   (control-flow simplification)
Found 69 errors.
```

Total ruff (all enabled): 214.

`tests/**` per-file-ignores already cover PLR2004 and PLR0913, so the 25+10 of those are all in src/. C901/PLR0912/0915/0911 may be in either.

## Strategy

- **R7a — PLR2004 (25)**: hoist magic values to module-level `Final` constants with descriptive names. If a value is genuinely a one-off literal (e.g. `if status == 200`), per-symbol `# noqa: PLR2004` is acceptable but rare — prefer extraction.
- **R7b — PLR0913 (10)**: each `>8 args` function gets a kwargs-bundle dataclass OR keyword-only with sensible defaults, OR — if the function is internal and will keep churning — raise `max-args` to a higher number (configured number must be in pyproject + documented in SUPPRESSIONS.md).
- **R7c — C901 + PLR0912 + PLR0915 + PLR0911 (34)**: complex functions. Two paths per function:
  1. **Refactor**: split into helpers; works when the function has clear sub-phases.
  2. **Pragmatic threshold**: if a function is irreducibly complex (e.g. a parser, a large match-block), raise `max-complexity` / `max-branches` / `max-statements` / `max-returns` in pyproject **with documented justification**.

  Default to refactor for ≤3 fixes; for the rest, the implementer chooses with judgment.

- **API style gate** is at zero — coordinate so signature changes (R7b) don't introduce violations of the `add_*`/`set_*` rule, the `position: Point` rule, etc.

## Done condition

- `uv run ruff check src tests --select PLR,C90` → 0.
- `api_style_gate.py` still 0 (signature rules).
- `fp_purity_gate.py` still 0.
- `import-linter` 0.
- `ruff format --check` 0.
- pytest 1827 passing.
- Suppressions logged in `docs/ratchet/SUPPRESSIONS.md`. No inline `# noqa` for PLR2004 unless truly one-off.
- One commit per sub-wave (R7a/b/c at minimum).
