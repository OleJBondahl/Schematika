# Wave R3 baseline — enable T20 / LOG / G / Q

Branch base: `branch1` @ `5cc9417`.

## Target

Enable 4 new ruff rule sets:

1. **T20** — flake8-print (no `print()` / `pprint()` in library code)
2. **LOG** — flake8-logging
3. **G** — flake8-logging-format
4. **Q** — flake8-quotes (ruff format already enforces double quotes — expected to be zero)

(PT was in the original plan list but is already enabled; dropped.)

## State at start

`uv run ruff check src tests --select T20,LOG,G,Q --statistics`:

```
4   T201   print
Found 4 errors.
```

LOG, G, Q all zero. The 4 prints are somewhere in src/ or tests/.

## Done condition

- pyproject `[tool.ruff.lint].select` has all 4 added.
- `uv run ruff check src tests --select T20` → 0 (4 prints either replaced with logging, removed if debug, or `# noqa: T201` with `Why:` in SUPPRESSIONS.md).
- `uv run ruff check src tests --select LOG,G,Q` → 0 (config-only, lock-in).
- pytest still passes.
- 4 ratchet gates still green.
- One commit per rule set.
