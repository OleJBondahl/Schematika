# Wave R8 baseline — S / DTZ / PTH / ERA / FBT / EM / TC / TID

Branch base: `branch1` @ `6e6fa08`.

## State at start

`uv run ruff check src tests --select S,DTZ,PTH,ERA,FBT,EM,TC,TID --statistics`:

```
3451  S101    assert
  97  PTH118  os-path-join
  78  PTH123  builtin-open
  62  EM102   f-string-in-exception
  44  S108    hardcoded-temp-file
  36  ERA001  commented-out-code
  35  PTH110  os-path-exists
  22  FBT001  boolean-type-hint-positional-argument
  19  TC001   typing-only-first-party-import
  18  EM101   raw-string-in-exception
  18  FBT002  boolean-default-value-positional-argument
  15  PTH103  os-makedirs
  14  PTH100  os-path-abspath
   9  PTH120  os-path-dirname
   9  TC006   runtime-cast-value
   7  PTH108  os-unlink
   6  TC003   typing-only-standard-library-import
   2  PTH109  os-getcwd
   2  PTH208  os-listdir
   2  S102    exec-builtin
   1  DTZ005  call-datetime-now-without-tzinfo
   1  FBT003  boolean-positional-value-in-call
   1  PTH112  os-path-isdir
   1  PTH117  os-path-isabs
   1  PTH119  os-path-basename
Found 3951 errors.
```

## Strategy — split into sub-waves

| Sub | Rule(s) | Approach | Expected Δ |
|-----|---------|----------|-----------|
| R8a | S | per-file-ignore S101 for `tests/**` (asserts are pytest's mechanism); inspect S102/S108 case-by-case | −3497 (pulls 3451 + most of S108) |
| R8b | DTZ | 1 fix (provide tzinfo) | −1 |
| R8c | PTH | `os.path` → `pathlib` migration; `--fix --unsafe-fixes` for the auto-fixable then manual review | −271 |
| R8d | ERA | remove commented-out code | −36 |
| R8e | FBT | per-file-ignore for tests if needed; for src/, convert positional bool params to keyword-only | −41 |
| R8f | EM | extract f-string into intermediate variable before `raise` | −80 |
| R8g | TC | move type-only imports under `if TYPE_CHECKING:`; auto-fixable for many | −34 |
| R8h | TID | lock-in only (already 0) | 0 |

After R8 the config has every standard ruff rule set turned on with `ignore = ["TRY003"]` only. `[per-file-ignores]` covers the small set of justified exceptions (tests/IEC naming, sandbox catches, asserts in tests).

## Done condition

- pyproject `[tool.ruff.lint].select` includes S, DTZ, PTH, ERA, FBT, EM, TC, TID.
- `uv run ruff check src tests` (default config, no select override) reports 0 violations of those rules.
- All four ratchet gates green.
- pytest 1827 passing.
- Suppressions logged.
