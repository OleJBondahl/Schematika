# Wave T0 baseline — Ty noise reduction

Branch base: `branch1` @ `8475eda` (post-R8 + PROGRESS update).

## State at start

`uv run ty check`:

```
82  error[unresolved-attribute]
40  error[invalid-argument-type]
32  error[unresolved-import]
25  error[missing-argument]
22  warning[unused-type-ignore-comment]
 2  error[positional-only-parameter-as-kwarg]
 2  error[call-non-callable]

Found 205 diagnostics
```

`uv run ty check src` reports **16** diagnostics (the rest are in `tests/`, `scripts/`, generated artefacts).

`tools/` is already excluded via `[tool.ty.src] exclude = ["tools/"]`.

## Scope

T0 is *noise reduction only* — clearing easy wins so T1+ can focus on real errors.

1. **Drop the 22 `unused-type-ignore-comment` warnings.** Each is a blanket `# type: ignore` directive ty no longer thinks is needed. Delete the comment if removal leaves ty silent at that line; if a different ty rule fires, replace with a narrow `# type: ignore[<rule>]` and note which.

2. **Audit `scripts/vulture_whitelist.py` (179 lines).** This file exists solely to keep vulture quiet — it's a list of bare attribute references against fictitious classes. ty has no business analysing it. Add it to `[tool.ty.src] exclude` and confirm the post-exclude diagnostic count drops by exactly the number of diagnostics that file contributes.

3. **(Stretch — optional) Audit `scripts/`.** If multiple files in `scripts/` produce noise that has nothing to do with shipped code (mutmut runners, codesight glue, etc.), exclude `scripts/` wholesale or per-file. Don't touch `scripts/api_style_gate.py` or `scripts/fp_purity_gate.py` — those *are* shipped infrastructure and should stay typed.

## Out of scope (defer to T1+)

- Any real error in `src/` (T1 territory).
- Real errors in `tests/` (T2/T3 territory).
- `unresolved-attribute` / `unresolved-import` / `invalid-argument-type` / `missing-argument` / `positional-only-parameter-as-kwarg` / `call-non-callable` — all are real diagnostics, not noise. Don't suppress them in T0.

## Done condition

- `unused-type-ignore-comment` count: 22 → 0.
- `scripts/vulture_whitelist.py` excluded from ty (configuration change in `pyproject.toml`).
- Total ty diagnostic count drops by ≥22 (probably more, depending on how much noise `vulture_whitelist.py` contributed).
- No new ty diagnostics introduced anywhere.
- `uv run ruff check src tests` — 172 violations (unchanged from R8 endpoint).
- All four ratchet gates green (`ruff format --check`, `api_style_gate.py`, `fp_purity_gate.py`, import-linter).
- `uv run pytest -q` — 1827 passing, 2 skipped, 12 pre-existing collection errors (unchanged baseline).
- New entries in `docs/ratchet/SUPPRESSIONS.md` for the `vulture_whitelist.py` exclusion (and any narrow `# type: ignore[<rule>]` introduced).

## Strategy

- One commit for the 22 deletions/narrowings (T0a).
- One commit for the ty config change excluding `vulture_whitelist.py` and any other scripts/ files justified (T0b).
- One docs commit for SUPPRESSIONS.md if needed (T0c).
