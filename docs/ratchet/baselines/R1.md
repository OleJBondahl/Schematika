# Wave R1 baseline — ruff `--fix` sweep

Branch base: `branch1` @ `f11f9f0`.

## Target

Drive ruff violation count down via `--fix` (safe) then `--fix --unsafe-fixes` (with review).

## State at start

`uv run ruff check src tests --statistics`:

- **Total: 241 errors**
- **4 auto-fixable** (`--fix`): I001 unsorted-imports
- **21 hidden auto-fixable** (`--fix --unsafe-fixes`): mostly type/idiom upgrades

Top categories (no fixers expected):
- B018 useless-expression: ~33 (likely test artifacts; tests may have intent)
- D-series docstring: ~80 combined (Wave R4 territory, do not touch here)
- PLR2004 magic-value: 24 (Wave R7 territory)
- N806 naming: 23 (Wave R5 territory)
- ARG/PLR0912/PLR0913/PLR0915 etc.: later waves

## Done condition

- `uv run ruff check src tests` count strictly less than 241.
- `uv run pytest -q` passes (no test regressions).
- No ratchet gate that's currently zero (`fp_purity_gate`, `api_style_gate`, `import-linter`, `ruff format --check`) regresses.
- Diff is justified hunk-by-hunk; nothing semantically risky merged blindly.
