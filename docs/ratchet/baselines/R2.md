# Wave R2 baseline — enable RUF / PERF / PIE / ICN / ISC

Branch base: `branch1` @ `b0f904e`.

## Target

Enable five new ruff rule sets, drive each to zero, commit one rule set per commit:

1. **RUF** — Ruff-specific rules (incl. RUF100 unused-noqa, RUF012 mutable-class-default)
2. **PERF** — perflint (incl. PERF101–402, list/dict comprehension perf, try/finally)
3. **PIE** — flake8-pie (incl. PIE790 unnecessary-pass, PIE808 unnecessary-range-start, PIE810 multiple-startswith)
4. **ICN** — flake8-import-conventions (`numpy as np`, `pandas as pd`, etc. — usually low-impact for this codebase)
5. **ISC** — flake8-implicit-str-concat (catches accidental string concatenation `"foo" "bar"`)

(SIM was already enabled — dropped from the original plan list.)

## Current ruff config

`pyproject.toml` `[tool.ruff.lint].select` already enables:
E, W, F, I, C, B, UP, SIM, FA, N, D, ARG, PLR, PT, RET, C90.

`ignore = []`.

## State at start

`uv run ruff check src tests` reports 220 errors (post-Wave-R1).

## Done condition

- pyproject `[tool.ruff.lint].select` has all 5 new rule sets added.
- `uv run ruff check src tests` reports `0` violations for each of `RUF`, `PERF`, `PIE`, `ICN`, `ISC` (combined; total ruff count of-other-rules unchanged from 220, ideally lower).
- `uv run ruff format --check src tests tools` still exit 0.
- pytest still passes (no test logic changes).
- `fp_purity_gate`, `api_style_gate`, `import-linter` still green.
- One commit per rule set: `refactor(wave-R2a): enable RUF`, `refactor(wave-R2b): enable PERF`, …
