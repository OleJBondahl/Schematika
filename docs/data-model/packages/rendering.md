# Package `rendering`

> Generated by `scripts/type_graph.py` from the live code. Do not edit; regenerate with `just type-graph`.

3 types.

## Structure inside the package

Fields, hub attributes and inheritance between this package's own types.

```mermaid
flowchart LR
    n_rendering_typst_compiler_TypstCompiler(["TypstCompiler"]):::pkg_rendering
    n_rendering_typst_compiler__Page["_Page"]:::pkg_rendering
    n_rendering_typst_compiler_TypstCompiler --> n_rendering_typst_compiler__Page
    classDef pkg_rendering fill:#eae5de,stroke:#8c6d46,color:#1d1f21
```

## Cross-package references

**Uses**

none

**Used by**

none

## Types

### TypstCompiler

`rendering.typst.compiler` - hub

Multi-page Typst PDF compiler.

| Uses | Kind | Via |
|---|---|---|
| `TypstCompilerConfig` | takes | `__init__` |
| `_Page` | holds | `_pages` |

### TypstCompilerConfig

`rendering.typst.compiler` - dataclass, mutable

Configuration for the Typst PDF compiler.

| Field | Type |
|---|---|
| `drawing_name` | `str` |
| `drawing_number` | `str` |
| `author` | `str` |
| `project` | `str` |
| `revision` | `str` |
| `logo_path` | `str \| None` |
| `font_family` | `str` |
| `root_dir` | `str` |
| `temp_dir` | `str` |
| `datetime_stamp` | `bool` |

Used by: `TypstCompiler`

### _Page

`rendering.typst.compiler` - dataclass, mutable

Internal representation of a page in the document.

| Field | Type |
|---|---|
| `page_type` | `str` |
| `title` | `str` |
| `svg_path` | `str` |
| `terminals_csv_path` | `str` |
| `csv_path` | `str` |
| `md_path` | `str` |
| `notice` | `str \| None` |
| `typst_content` | `str` |
| `terminal_titles` | `dict[str, str] \| None` |
| `cable_svg_paths` | `list[tuple[str, str, str, str]] \| None` |
| `cable_toc_entries` | `list[tuple[str, str, str]] \| None` |

Used by: `TypstCompiler`
